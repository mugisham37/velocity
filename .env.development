# Node Environment
NODE_ENV=development
PORT=4000

# Database Configuration
DATABASE_URL=postgresql://postgres:moses@localhost:5432/velocity

# JWT Configuration (Use these for development only, change in production)
JWT_SECRET=this_is_a_very_long_secret_key_for_development_32_characters_minimum
JWT_REFRESH_SECRET=this_is_a_very_long_refresh_secret_key_for_dev_32_characters_minimum

# SMTP Configuration
SMTP_USER=developer@velocity-erp.com
SMTP_PASS=your_smtp_password
SMTP_HOST=localhost
SMTP_PORT=1025

# Redis Configuration (can be updated later)
REDIS_URL=redis://localhost:6379

# Elasticsearch Configuration (can be updated later)
ELASTICSEARCH_URL=http://localhost:9200

# S3/MinIO Configuration (can be updated later)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET_NAME=velocity

# TIMESCALE Configuration
TIMESCALE_URL=postgresql://postgres:moses@localhost:5432/velocity_timescale

# Application URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:4000
