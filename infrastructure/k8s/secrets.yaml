apiVersion: v1
kind: Secret
metadata:
  name: kiro-secrets
  namespace: kiro-erp
type: Opaque
stringData:
  database-url: "postgresql://kiro_user:CHANGE_ME@postgres:5432/kiro_erp"
  redis-url: "redis://:CHANGE_ME@redis:6379"
  jwt-secret: "CHANGE_ME_TO_SECURE_JWT_SECRET"
  jwt-refresh-secret: "CHANGE_ME_TO_SECURE_JWT_REFRESH_SECRET"
  postgres-password: "CHANGE_ME"
  redis-password: "CHANGE_ME"
  elastic-password: "CHANGE_ME"
  minio-access-key: "CHANGE_ME"
  minio-secret-key: "CHANGE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: kiro-tls-secret
  namespace: kiro-erp
type: kubernetes.io/tls
data:
  # Base64 encoded certificate and key
  # Replace with actual certificate data
  tls.crt: LS0tLS1CRUdJTi...
  tls.key: LS0tLS1CRUdJTi...
---
apiVersion: v1
kind: Secret
metadata:
  name: regcred
  namespace: kiro-erp
type: kubernetes.io/dockerconfigjson
data:
  # Base64 encoded Docker registry credentials
  # Replace with actual registry credentials
  .dockerconfigjson: eyJhdXRocyI6e319